{
  "name": "my-custom-validators",
  "version": "1.0.2",
  "description": "A custom validation library for Node.js using class-validator, standardizing the error messages that will be sent to the system that consumes the service.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.5",
    "typescript": "^5.4.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AliAhmadHassan/my-custom-validators.git"
  }
}
